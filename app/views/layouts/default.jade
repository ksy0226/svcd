//일반사용자
- if(session.user_flag ==1 || session.user_flag ==3 || session.user_flag ==4){ 
    extends ../layouts/user
- }

//팀장
- if(session.user_flag ==5){ 
    extends ../layouts/deptadmin
- }

//업무담당자
- if(session.user_flag !=9){
    extends ../layouts/admin
-}

